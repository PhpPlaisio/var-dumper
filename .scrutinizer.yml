filter:
  dependency_paths:
    - "vendor/"
  excluded_paths:
    - 'test/'


build:
  nodes:
    analysis:
      tests:
        override:
          - command: './bin/phing unit'
            coverage:
              file:   'test/coverage.xml'
              format: 'php-clover'
